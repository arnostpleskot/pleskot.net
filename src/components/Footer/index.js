// @flow
import * as React from 'react'
import { StyledFooter, StyledFooterEmail } from './styled'

const Footer = ({ children }) => <StyledFooter>{children}</StyledFooter>

export const FooterEmail = StyledFooterEmail

export default Footer
